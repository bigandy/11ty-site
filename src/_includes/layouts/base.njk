{% include "components/head.njk" %}
<body {% if templateClass %} class="{{ templateClass }}" {% endif %}>



<svg width="0" height="0">
  <filter id="grainy" x="0" y="0" width="100%" height="100%">
    <feTurbulence type="fractalNoise" baseFrequency=".537"></feTurbulence>
    <feColorMatrix type="saturate" values="0"></feColorMatrix>
    <feBlend in="SourceGraphic" mode="multiply"></feBlend>
  </filter>
</svg>

<div class="container">

  {% include "components/header.njk" %}

  <main>

      {{ content | safe }}

  </main>

  {% include "components/footer.njk" %}

  {# {% include "components/notification.njk" %} #}

  {# {% set js %}
  {% include '../../assets/js/theme-toggle.js' %}
  {% endset %}

  {% if metadata.isProduction %}
		<script>
			{{ js | jsmin | safe }}
		</script>
	{% else %}
		<script src="/assets/js/theme-toggle.js" async defer type="module" />
	{% endif %} #}
</div>
  <div class="background"></div>

</body>
</html>
